
# mycursor.execute("create table nini(id INT,ggg VARCHAR(25))")
# table = 'stocks'
# mycursor.execute( f"SELECT * FROM {table}\
#                   WHERE NOT id > 1\
#                  ")

# mycursor.execute( 
#                   'INSERT INTO stocks'
#                   '(name, price, quantity)'
#                   'VALUES ( "hmd", 0.0, 99999)'

# )

# mycursor.execute( "SELECT Column_name FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='stocks' ")

# mycursor.execute( "SHOW COLUMNS FROM stocks")
